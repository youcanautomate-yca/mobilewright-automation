import type { HardwareButton, MobilewrightDriver, ScreenshotOptions, SwipeDirection, SwipeOptions, ViewNode } from '@mobilewright/protocol';
import { Locator, type LocatorOptions } from './locator.js';
export declare class Screen {
    private readonly driver;
    private readonly root;
    constructor(driver: MobilewrightDriver, locatorDefaults?: LocatorOptions);
    getByLabel(label: string, opts?: {
        exact?: boolean;
    }): Locator;
    getByTestId(testId: string): Locator;
    getByText(text: string | RegExp, opts?: {
        exact?: boolean;
    }): Locator;
    getByType(type: string): Locator;
    getByRole(role: string, opts?: {
        name?: string | RegExp;
    }): Locator;
    getByPlaceholder(placeholder: string, opts?: {
        exact?: boolean;
    }): Locator;
    screenshot(opts?: ScreenshotOptions): Promise<Buffer>;
    swipe(direction: SwipeDirection, opts?: SwipeOptions): Promise<void>;
    pressButton(button: HardwareButton): Promise<void>;
    tap(x: number, y: number): Promise<void>;
    goBack(): Promise<void>;
    viewTree(): Promise<ViewNode[]>;
}
//# sourceMappingURL=screen.d.ts.map