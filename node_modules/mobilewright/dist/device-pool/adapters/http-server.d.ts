import type { DevicePool } from '../application/device-pool.js';
export interface DevicePoolHttpServerOptions {
    pool: DevicePool;
}
export declare class DevicePoolHttpServer {
    private readonly pool;
    private readonly server;
    private readonly responsesByAllocationId;
    constructor(options: DevicePoolHttpServerOptions);
    listen(): Promise<number>;
    close(): Promise<void>;
    private handle;
    private handleIsAppInstalled;
    private handleRecordAppInstalled;
    private handleShutdown;
    private handleAllocate;
    private handleRelease;
}
//# sourceMappingURL=http-server.d.ts.map