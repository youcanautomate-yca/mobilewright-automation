import type { AllocationCriteria, AllocationHandle, DevicePoolClient } from '../application/ports.js';
export interface HttpDevicePoolClientOptions {
    baseUrl: string;
}
export declare class HttpDevicePoolClient implements DevicePoolClient {
    private readonly baseUrl;
    private readonly agent;
    private readonly openAllocateRequests;
    constructor(options: HttpDevicePoolClientOptions);
    allocate(criteria: AllocationCriteria): Promise<AllocationHandle>;
    release(allocationId: string): Promise<void>;
    isAppInstalled(allocationId: string, bundleId: string): Promise<boolean>;
    recordAppInstalled(allocationId: string, bundleId: string): Promise<void>;
    private postJson;
}
//# sourceMappingURL=http-client.d.ts.map